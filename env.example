# 服务器配置
PORT=8080
ENVIRONMENT=development
# BASE_URL=http://yourdomain.com:8080  # 生产环境下请设置正确的域名，开发环境会自动使用 http://localhost:8080

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
# 必须设置：数据库密码（生产环境至少12位，包含大小写字母、数字、特殊字符）
DB_PASSWORD=
DB_NAME=domain_manager
DB_TYPE=postgres

# JWT配置
# 必须设置：JWT密钥（至少64位字符，建议使用随机生成的强密钥）
# 生成方法: openssl rand -base64 64 | tr -d "=+/" | cut -c1-64
JWT_SECRET=

# 加密配置  
# 必须设置：AES加密密钥（64个十六进制字符，用于加密敏感数据如SMTP密码）
# 生成方法：openssl rand -hex 32
ENCRYPTION_KEY=

# 邮件配置 (可选)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=noreply@yourdomain.com

# DNSPod配置 (可选，也可在管理后台配置)
DNSPOD_TOKEN=your_dnspod_token_here
