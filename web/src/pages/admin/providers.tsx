import { Typography } from 'antd'

const { Title } = Typography

export default function AdminProviders() {
  return (
    <div>
      <Title level={2}>DNS 提供商管理</Title>
      <p>DNS 提供商管理功能开发中...</p>
    </div>
  )
}