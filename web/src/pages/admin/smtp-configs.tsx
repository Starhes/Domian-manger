import { Typography } from 'antd'

const { Title } = Typography

export default function AdminSMTPConfigs() {
  return (
    <div>
      <Title level={2}>SMTP 配置管理</Title>
      <p>SMTP 配置管理功能开发中...</p>
    </div>
  )
}