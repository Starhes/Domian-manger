import { Typography } from 'antd'

const { Title } = Typography

export default function DNSRecords() {
  return (
    <div>
      <Title level={2}>DNS 记录</Title>
      <p>DNS 记录管理功能开发中...</p>
    </div>
  )
}